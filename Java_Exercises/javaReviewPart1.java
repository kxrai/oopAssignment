class javaReviewPart1 {
  public static void main(String[] args) {
    /**
      In Java, we primarily use a handful of data types
      * String: A collection of characters in between ""
      * Int: Any whole number
      * Double: Any number with a decimal
      * Boolean: True or False
      * Char: Characters (specifically single characters). These are in between ''
      *
      * Other?
      * Long, short
      */

    // Java is a strongly typed language
    /**
      * To declare any variable in Java, declare the
        * data type, variable name, and value all at the same time
        *
        * ex.
          * dataType, variableName = value;
      */

    /* 
    String school = "Bill Hogarth S.S";

    //String is dataType, school is variable, value is "Bill Hogarth S.S/"

    //We do NOT have to assign a value to a variable as it is declared
    String address;
    address = "100 Donald Sim Ave.";

    System.out.println(address);

    int x, y;
    x = 1;
    y = 2;

    Double pi = 3.14;
    Double three = 3.0;

    // no quotation marks
    // if you add quotation marks, it becomes a string
    boolean on = true;
    boolean off = false;

    char a = 'a';
    char thing = '!';
    
    System.out.println("Hey there. You look like you need some luck. That'll be $50. Hehehe I took your money sucker");
    // To call the variable, we just need to type its name inside the print function's paramaters
    System.out.println(school);

    //System.out.println(x + y);
    //System.out.println(school + address);

    //Integer Integer Division returns an INT (Rounded DOWN to the NEXT WHOLE NUMBER)

    //However, when you multiply/divide an int by a double, it returns a double

    
    System.out.println(y/3);
    
    System.out.println(y/3.0);
    */
    
  }
}